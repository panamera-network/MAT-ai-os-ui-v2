# domain

Pure types and rules — no React, no fetch, no I/O. This is where the VISION
API's data contracts (health, agents, loops, skills, models, soul, identity,
memory, services, etc.) get modeled as plain TypeScript, independent of how
they're fetched or rendered.
