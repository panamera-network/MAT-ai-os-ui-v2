# screens

Top-level views the user navigates between. Composed from `components/`,
fed by an adapter from `adapters/` via `app/`.

`HomeScreen.tsx` is the first: presence/identity, core health/status, and
the one primary activity area (talking to MAT). Agents/memory/governance/
MCP/skills/models are deliberately not screens yet.
